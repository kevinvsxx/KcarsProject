@model IEnumerable<Project.Models.Piloot>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Voornaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Auto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Voornaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Auto.Merk)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PilootID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PilootID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PilootID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
